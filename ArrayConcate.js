//The Concate method creates a new array by attaching elements from the array given as an argument to the original array element.The method changes neither the original array nor the array specified as argument.

let names=["Olivia", "Emma", "Mateo", "Samuel"];
let otherNames=["William", "Jane"];
let allNames=names.concat(otherNames);

console.log(names);

console.log(otherNames);
console.log(allNames);
